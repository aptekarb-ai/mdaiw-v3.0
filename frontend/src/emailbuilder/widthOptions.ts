export const EMAIL_WIDTH_PRESETS: number[] = [600, 650, 700, 750, 800];

export const DEFAULT_EMAIL_WIDTH = 700;

// Matches backend/emailbuilder/models.py MIN_EMAIL_WIDTH / MAX_EMAIL_WIDTH —
// keep these two in sync if either changes.
export const CUSTOM_WIDTH_MIN = 320;
export const CUSTOM_WIDTH_MAX = 1200;
