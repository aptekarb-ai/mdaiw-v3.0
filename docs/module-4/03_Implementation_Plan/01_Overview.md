# 01 — Overview

Module-4 is an AI-assisted email authoring environment built around a portable generic core. Users can create emails visually from reusable modules, tune responsive behavior, edit code, validate compatibility, preview client rendering and export for multiple marketing platforms.

The builder should prioritize production email constraints over web-page conventions. The generated email remains table-first and conservative by default.
